#include "math.hpp"
#include <bits/stdc++.h>
using namespace std;

#define fastio ios::sync_with_stdio(false); cin.tie(nullptr);

typedef long long ll;

int main(){
    fastio;

    // 3. DADO N CARAMELOS, Y 2 NIÑOS, DARLE 1 A CADA NIÑO MINIMO Y LA CANTIDAD QUE RECIBEN DEBE SER DISTINTA.
    // DEVOLVER: numero de formas posibles de repartilo
    // Verificar que n sea mayor a 2
    int n = 7;
    int formas = 0;
    if (n < 2) cout << "0";
    else {

    };

    return 0;
}